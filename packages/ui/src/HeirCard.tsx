import type { Heir, HeirShare } from '@mirath/core'

interface Props {
  heir: Heir
  share?: HeirShare
}

/**
 * Displays an heir's details and their computed inheritance share.
 * TODO: implement.
 */
export function HeirCard(_props: Props) {
  return null
}
