/**
 * Admin dashboard home — shows license overview stats.
 *
 * Fetches from license-server API (env: LICENSE_SERVER_URL).
 * Protected by ADMIN_TOKEN bearer auth.
 *
 * TODO: implement license list, stats, and revocation UI.
 */
export default function AdminPage() {
  return (
    <main>
      <h1>Mirath Admin</h1>
      <p>Boilerplate — implement dashboard.</p>
    </main>
  )
}
